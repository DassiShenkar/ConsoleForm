#pragma once
enum  class Keys { UP, DOWN, LEFT, RIGHT, BACKSPACE, ENTER, TAB, SPACEBAR, DEL, INSERT, OTHER, KEY_RELEASED };

enum class Color { Black, Blue, Green, Red, Cyan, Purple, Orange, White };

enum class BorderType { Single, Double, None };